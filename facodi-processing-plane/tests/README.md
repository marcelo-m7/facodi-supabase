# Tests

This repository will own Supabase-focused validation.

Bootstrap scope:

- preserve live function snapshots;
- verify extracted functions remain reproducible from those snapshots;
- add migration and worker tests as the queue-driven v3 contract is implemented.
